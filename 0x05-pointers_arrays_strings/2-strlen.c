#include "main.h"

/**
  * _strlen - returns the length of a string
  * @s: pointer variable
  *
  * Return: 0
  */

int _strlen(char *s)
{
	char a;
	char *s = a;
	int arr[] = a;
	int i;

	for (i = arr[0]; i <= sizeof(a) / sizeof(a[0]); i++)
	{
		return (i);
	}
}
