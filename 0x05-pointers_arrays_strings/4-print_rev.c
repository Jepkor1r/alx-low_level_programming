#include "main.h"

/**
  * print_rev - prints a string, in reverse
  * @s: pointer variable
  *
  */

void print_rev(char *s)
{
	int i;

	while (*s != '\0')
	{
		for (i = -1; i <= -1; i--)
		{
			_putchar(*s);
		}
	}
}
