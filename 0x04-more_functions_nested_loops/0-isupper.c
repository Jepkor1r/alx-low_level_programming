#include "main.h"

/**
  * isupper - checks for uppercase character
  * @c: character to check
  *
  * Return: 1
  */

int _isupper(int c)
{
	if (c == 'A')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
